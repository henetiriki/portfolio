{
  "hooks": {
    "PostToolUse": [
      {
        "hooks": [
          {
            "command": "git add -- $CLAUDE_FILE_PATHS 2>/dev/null; yarn lint-staged",
            "statusMessage": "Linting the edited file",
            "timeout": 60,
            "type": "command"
          }
        ],
        "matcher": "Edit|Write"
      }
    ]
  },
  "permissions": {
    "allow": [],
    "ask": ["Bash(gh pr merge *)"]
  }
}
