import { createStyles } from '@mantine/core';

export const useWaveWrapperStyles = createStyles({
  waveWrapper: {
    height: '10rem',
    lineHeight: 0,
    overflow: 'hidden',
    width: '100%',
  },
  waveWrapperBottom: {
    marginBottom: '8rem',
  },
});
